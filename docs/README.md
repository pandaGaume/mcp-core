# docs/

This directory is served as the project landing page via GitHub Pages.

Source files for the site live here. Do not rename or move this folder: GitHub Pages requires the source to be either the repo root or a folder named `docs`.

To update the site, edit `index.html` and push to `main`.
